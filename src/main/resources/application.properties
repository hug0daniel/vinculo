spring.application.name=vinculo

# Server
server.port=8080

# H2 Console (dev only)
spring.h2console.enabled=true
spring.h2console.path=/h2console

# JPA
spring.jpa.show-sql=false
spring.jpa.open-in-view=false